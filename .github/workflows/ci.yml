name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
       
      - name: Run basic Checks
        run: |
          echo "✅ Repo structure check passed!"
          ls -R .